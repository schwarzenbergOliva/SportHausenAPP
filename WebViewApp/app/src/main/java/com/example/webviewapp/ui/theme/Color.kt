package com.example.webviewapp.ui.theme

import androidx.compose.ui.graphics.Color

// --- Paleta de marca SportHausen (alineada con la app web) ---

val BrandNavy = Color(0xFF16243C)        // Primario / estructura / foco de campos
val BrandNavyDeep = Color(0xFF0E1726)    // Cima del gradiente (navy más profundo)
val BrandTeal = Color(0xFF2BB0A3)        // Secundario / acento del logo
val BrandCoral = Color(0xFFE25C3E)       // Acento / botón principal (CTA)
val BrandCoralDark = Color(0xFFC44A2E)   // Coral presionado / hover

// Neutros
val NeutralLight = Color(0xFFF8FAFC)     // Casi blanco / superficies
val NeutralDark = Color(0xFF1E293B)      // Gris pizarra / texto oscuro y cierre del gradiente
val NeutralVariant = Color(0xFF5A6B7B)   // Texto secundario / iconos sin foco
val BrandError = Color(0xFFC62828)

// --- Variante oscura ---
val BrandNavy80 = Color(0xFFAFC0D6)
val BrandTeal80 = Color(0xFF8FE3D8)
val BrandCoral80 = Color(0xFFF4A593)
