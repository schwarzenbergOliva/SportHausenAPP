package com.example.webviewapp

import android.app.Application

/**
 * Clase Application del proyecto. Punto único de inicialización a nivel de app
 * (analytics, crash reporting, o el grafo de DI cuando se añada Hilt).
 */
class WebViewApplication : Application()
