<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Configuración SOLO para builds debug.
        Permite tráfico cleartext (HTTP) hacia el host del emulador (10.0.2.2),
        que es como Android Emulator alias-ea la PC del desarrollador.

        En release sigue activa la config estricta de main/, que prohibe HTTP.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
